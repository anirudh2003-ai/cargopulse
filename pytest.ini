[pytest]
testpaths = tests

markers =
    unit: fast tests with no external services
    integration: tests requiring PostgreSQL or multiple components
    regression: tests protecting validated CargoPulse outputs

addopts = -ra
